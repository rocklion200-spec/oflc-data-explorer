# Production data host: a separate Pages repo so app deploys don't invalidate
# the CDN cache of the ~840 MB of parquet (see README "Data hosting").
VITE_DATA_BASE=https://rocklion200-spec.github.io/oflc-data/
